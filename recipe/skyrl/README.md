# SkyRL Integration Recipe

This recipe integrates SkyRL into VeRL.

## Usage

To run the SkyRL integration, you can use the `verl_main_ppo.py` script.

```bash
python3 -m recipe.skyrl.verl_main_ppo \
    ...
```

See `SkyRL/skyrl-agent/examples/run_verl/verl_oh.sh` for an example configuration.
